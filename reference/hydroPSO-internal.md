# Internal hydroPSO objects

Internal hydroPSO objects.

## Details

These are not to be called by the user.

## Value

It does not return a value. It is only sued to declare internal package
objects, which are not to be called by the user
